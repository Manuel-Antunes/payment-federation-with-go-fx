// Package ent é o cliente ent (entgo.io) do bounded context de PAGAMENTOS
// (tabelas payment e order). Geração por módulo: schema e client próprios.
package ent

//go:generate go run -mod=mod entgo.io/ent/cmd/ent generate ./schema
